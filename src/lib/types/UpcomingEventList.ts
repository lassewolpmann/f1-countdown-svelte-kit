export interface UpcomingEvent {
    latitude: number,
    localeKey: string,
    location: string,
    longitude: number,
    name: string,
    round: number,
    sessions: object,
    sessionsTableVisible?: boolean,
    slug: string
}