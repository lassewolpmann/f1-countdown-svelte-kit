export interface UpcomingEvent {
    latitude: number,
    localeKey: string,
    location: string,
    longitude: number,
    name: string,
    round: number,
    sessions: object,
    sessionsTableHidden?: boolean,
    slug: string
}